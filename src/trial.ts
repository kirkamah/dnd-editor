/**
 * Пробная версия (trial). Флаг подставляется на этапе сборки через Vite define
 * (__TRIAL__): полная сборка -> false, `npm run dist:trial` -> true.
 * Ограничения пробной версии DnD Editor:
 *   • открываются только записи не длиннее 1 часа;
 *   • нельзя добавлять свою музыку;
 *   • нельзя менять громкость голоса участника (дорожка/реплика/мьют);
 *   • нельзя делать плавный переход прозрачности фона (fade у ключа сцены);
 *   • нельзя менять аватары участников прямо в приложении.
 * Полная версия — по подписке на Boosty.
 */
declare const __TRIAL__: boolean;

export const IS_TRIAL: boolean = __TRIAL__;

/** Пробная версия открывает записи не длиннее одного часа. */
export const TRIAL_MAX_DURATION_MS = 60 * 60 * 1000;

export const BOOSTY_URL = 'https://boosty.to/no.harm.org';
